#include <RZEnginePCH.h>

namespace rczEngine
{
	void Music::Play()
	{
		//SoundAPI::Instance().PlayMusic(m_MusicCore);
	}

	void Music::Load(char* filePath, bool addToResourceManager)
	{
		//SoundAPI::Instance().CreateMusic(filePath, m_MusicCore);
	}
}