#include <RZEnginePCH.h>

namespace rczEngine
{
	void Sound::Play()
	{
		//SoundAPI::Instance().PlaySnd(m_SoundCore);
	}

	void Sound::Load(char* filePath, bool addToResourceManager)
	{
		//SoundAPI::Instance().CreateSound(filePath, m_SoundCore);
	}
}